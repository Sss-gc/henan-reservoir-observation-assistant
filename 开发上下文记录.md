# 河南省省控水库遥感观测平台：开发上下文记录

更新时间：2026-08-27

## 一、当前完成状态

### 1. 空间主数据

- 已从 `F:\AAAAPostgraduate\AAAGIS\地图资源` 中的水库 Shapefile 整理25座省控水库。
- 已生成 `public/data/reservoirs.geojson`，包含水库名称、代码、城市、代表点、WGS84经纬度、水面几何面积和完整 Polygon/MultiPolygon。
- 已叠加河南省界及18个省辖市边界。
- 已完成本地几何、OSM/Nominatim名称和行政区的交叉核验。
- 白沙水库等跨市水库保留跨区说明；面积为当前SHP水面面积，不是库容或流域面积。

### 2. 前端网页

- Vue 3 + TypeScript + Vite + Leaflet 响应式页面已经完成。
- 支持水库搜索、地图点击、缩放定位、移动端侧栏和空间属性展示。
- 天气区域显示16天数据，每张卡片同时显示星期和日期。
- 天气区域支持左右按钮和鼠标滚轮横向查看后续日期。
- 卫星窗口按卫星筛选，并结合天气生成透明规则评分。
- 实际产品区域显示平台、北京时间、分辨率、云量、完整覆盖率、详情和下载入口。
- 产品区增加“局部云量”按钮：对最近3组Sentinel-2产品读取20米SCL，按水库多边形裁剪并计算局部云、云影、雪和综合云障率。
- 已计算局部指标的产品显示局部云量、云障率及观测质量分；未计算产品仍明确显示为“整景云量”。
- Sentinel-2同次过境的多个官方快速预览会按瓦片地理位置拼成一张RGB预览，叠加水库边界并缓存为PNG；USGS Landsat缩略图目前会跳转登录页，因此加载失败后显示本地生成且明确标注的“覆盖示意图”，不会冒充真实遥感影像。
- Sentinel拼接事件提供JSON瓦片清单，包含每个组成产品、官方目录链接和可下载资产地址。

### 3. 后端与数据库

- FastAPI + SQLAlchemy + SQLite 后端已经完成，API前缀为 `/api/v1`。
- SQLite文件：`backend/data/reservoir-observer.db`。
- 已建立天气缓存、卫星目录、OMM星历、轨道窗口、实际影像产品和任务记录表。
- Open-Meteo天气由后端代理并缓存，失败时可使用最近成功缓存。
- Swagger文档地址：`http://127.0.0.1:8000/docs`。

### 4. 未来轨道窗口

- 已接入CelesTrak OMM星历，使用Skyfield/SGP4传播轨道。
- 当前启用 Sentinel-2A、Sentinel-2B、Sentinel-2C、Landsat 8、Landsat 9。
- 已过滤太阳高度角不大于10°的夜间过境。
- 使用“地面轨迹 + 传感器幅宽走廊 + 完整水库多边形相交”计算覆盖率。
- 只有覆盖率达到99.9%以上的窗口才入库，对外统一显示为100%完整覆盖。
- 最近一次30天重算：5颗卫星、309条完整覆盖窗口、无警告。
- 这些记录仍是轨道几何预测，不代表运营方已经安排成像。

### 5. 已获取的实际影像

- Sentinel-2 L2A来自 Copernicus Data Space Ecosystem 官方STAC。
- Landsat Collection 2 Level-2 Surface Reflectance来自USGS LandsatLook官方STAC。
- 实际产品保存产品编号、平台、拍摄时间、整景云量、分辨率、足迹、缩略图、目录和下载入口。
- 历史产品同样执行完整覆盖硬条件：产品足迹或同次过境瓦片并集必须覆盖水库99.9%以上。
- Sentinel-2按同一平台、同一分钟的相邻瓦片组成一次拼接覆盖事件。
- 丹江口水库最近一次90天查询保存19组完整覆盖数据：8组Sentinel-2四瓦片拼接、11景Landsat，全部覆盖率为100%。
- 其他水库在网页首次选择时按需查询；缓存未过期时不会重复访问官方目录。
- 已完成25座水库最近90天的批量预热：共保存871组完整覆盖产品，25/25座均有数据，批量任务无失败。
- 批量状态接口：`GET /api/v1/imagery/status`；批量刷新接口：`POST /api/v1/imagery/refresh-all`。

### 6. 当前验证结果

- 后端自动测试：17项通过；仅有Starlette TestClient关于httpx未来版本的弃用警告。
- 前端TypeScript检查及生产构建通过。
- 浏览器验收结果：16张天气卡片、16个日期、左右按钮滚动距离正常；Sentinel-2瓦片拼接可见；Landsat覆盖示意回退可正常加载。
- Sentinel多瓦片预览实测输出640×480 PNG；首次生成后缓存在 `backend/data/product-previews/`。
- 丹江口最近3组Sentinel-2局部质量已实算：局部云量分别为13.52%、22.40%和98.93%；全部基于约215万个水库范围有效SCL像元。

## 二、主要运行方式

### 一键启动

双击项目根目录的 `启动网站.bat`，然后访问：

- 网页：`http://127.0.0.1:5173/`
- 后端文档：`http://127.0.0.1:8000/docs`

### 刷新未来轨道

双击 `刷新卫星轨道.bat`，或者运行：

```powershell
.\.venv\Scripts\python.exe -m backend.app.tasks.refresh_orbits --days 30
```

### 刷新某座水库实际影像

```text
POST /api/v1/reservoirs/{水库ID}/products/refresh?days=90&limit_per_source=200
```

### 批量刷新25座水库

双击 `刷新全部影像目录.bat`，或运行：

```powershell
.\.venv\Scripts\python.exe -m backend.app.tasks.refresh_all_imagery --days 90 --limit-per-source 500
```

## 三、主要代码位置

- `src/App.vue`：网页交互、天气、未来窗口和实际产品显示。
- `src/styles.css`：全部页面样式和响应式布局。
- `backend/app/main.py`：FastAPI路由。
- `backend/app/models.py`：数据库表。
- `backend/app/services/orbit.py`：OMM、SGP4、太阳高度角和完整覆盖算法。
- `backend/app/services/imagery.py`：两个官方STAC目录、Sentinel瓦片组合和实际产品落库。
- `scripts/prepare_spatial_data.py`：Shapefile清理、面积/代表点/行政区计算与GeoJSON导出。

## 四、尚未实现或需要继续完善的功能

1. **真实Landsat像素缩略图**：USGS当前缩略图和COG直链会跳转EROS登录。页面目前显示明确标注的覆盖示意图。后续需要配置合法USGS账号/授权，或接入能覆盖当前产品时效的公开渲染服务。
2. **多瓦片自动打包**：已能下载包含全部组成瓦片和官方资产地址的JSON清单，但尚未由服务器代为下载并压缩全部原始文件；Copernicus资产仍需要用户合法授权。
3. **Landsat水库局部云量**：Sentinel-2 SCL局部云量已实现；Landsat QA_PIXEL位定义已经核准，但USGS当前数据文件访问需要授权，因此Landsat局部云量尚未自动计算。
4. **实际成像计划确认**：未来窗口只证明轨道和幅宽可以完整覆盖，未接入卫星运营方任务计划，不能视为已确认成像。
5. **国产卫星实际产品自动检索**：HJ-2B和高分一号宽幅轨道预测已实现，但国家遥感数据与应用服务平台需要账号，尚未获得公开文档化匿名产品API或项目授权凭据。
6. **生产部署**：尚未完成Nginx/HTTPS、用户权限、日志轮转、数据库备份和正式服务器部署。

## 五、建议的下一步顺序

1. 配置合法USGS数据访问后补齐Landsat QA_PIXEL局部云量。
2. 完善部署方案和论文所需系统架构/实验验证材料。

## 六、2026-08-27 阶段更新（后端基础设施）

- 系统版本升级到 `0.6.0`。
- Copernicus与USGS STAC查询已支持官方`next`分页、产品ID去重、429/5xx及网络失败指数退避重试，单来源上限提升到1000，批量默认500。
- 已接入APScheduler：轨道每2小时刷新；25座水库影像目录每天北京时间02:30刷新；任务均限制单实例并合并错过的执行。
- 新增 `GET /api/v1/scheduler/status`；实测调度器正在运行且能返回两项任务的下次执行时间。
- 已接入Alembic，基线版本为 `20260827_0001`；现有SQLite数据库已执行 `stamp head`。
- 新增分页、去重、服务端失败重试及调度状态测试；当前后端15项测试全部通过。
- 已用两个官方STAC进行14天小范围联网验证：Copernicus返回3项，USGS返回1项。

## 七、2026-08-27 阶段更新（水体指数与面积）

- 系统版本升级到 `0.7.0`；Alembic升级到 `20260827_0002`。
- 新增 `product_water_metrics`，保存逐产品NDWI/MNDWI、有效像元、水体面积、相对SHP变化、方法分歧和可信度。
- 使用公开Sentinel-2 L2A COG的B03/B08/B11/SCL，按资产元数据执行 `DN×0.0001−0.1`，并统一到20米网格。
- 采用SCL排除无效、云、云影和雪；完全无无云有效像元时拒绝输出面积。
- 水面主估计为MNDWI>0，另存NDWI>0结果用于交叉判断；面积按无云像元水体比例归一到SHP参考范围。
- 新增水面时序读取、最近影像计算接口及网页折线图，显示最新面积、相对变化、无云比例与高/中/低可信度。
- 彰武水库实测：最近3景中2景成功、1景因完全无无云有效像元被拒绝；2026-08-24结果为3.18 km²，对比SHP 3.75 km²为-15.3%，无云比例25.1%，明确标为低可信度。
- 页面浏览器验收通过；前端生产构建通过；后端17项测试通过。

## 八、2026-08-27 阶段更新（可视化管理后台）

- 系统版本升级到 `0.8.0`。
- 新增 `GET /api/v1/admin/overview` 与 `GET /api/v1/admin/tasks`，统一输出统计、调度、存储、新鲜度和任务信息。
- 网页顶部新增“管理后台”弹窗，10秒自动轮询，支持手动刷新状态、提交30天轨道刷新和25座影像批量刷新。
- 当前管理统计实测：25/25座有影像，共871景，其中Sentinel-2为547景、Landsat为324景；未来覆盖窗口309条；局部云量3景、水体面积2景。
- 存储状态实测：SQLite约68.2 MB；预览缓存12个文件、约6.1 MB；天气缓存覆盖10/25座。
- 增加独立异常任务列表，较早失败记录不会被大量成功记录覆盖；当前正确识别1条历史初始化失败任务。
- 管理后台未增加自动删除功能，避免误删数据库或缓存；只提供只读监控和既有刷新任务入口。
- 桌面浏览器可视验收通过；前端生产构建通过；后端19项测试通过。

## 九、2026-08-27 阶段更新（HJ-2B与高分一号）

- 系统版本升级到 `0.9.0`。
- 官方资料确认HJ-2B为4台16米多光谱相机拼接、实测幅宽优于800 km；高分一号WFV为16米、800 km幅宽。
- CelesTrak当前OMM验证：HJ-2B为NORAD 46479/2020-067B；GAOFEN-1为NORAD 39150/2013-018A。
- 两颗卫星均以800 km保守宽幅模式启用OMM/SGP4完整覆盖预测，仍明确标注不代表运营方已排程成像。
- 完成7颗卫星未来30天重算：共882条完整覆盖窗口，无警告；高分一号300条、HJ-2B 269条。丹江口两颗星各11条。
- 新增 `GET /api/v1/domestic-satellites` 及网页“国产卫星接入状态”卡片；观测窗口筛选已出现Gaofen-1和HJ-2B。
- 官方产品入口接到国家遥感数据与应用服务平台，但本次未发现公开文档化匿名产品目录API；实际产品仍需合法账号/接口凭据，页面不会生成虚假记录。
- 详细核验记录：`data/source_validation/domestic-satellites.md`。
- 页面浏览器验收通过；前端生产构建通过；后端新增国产预测语义测试后共21项。

## 十、2026-08-27 阶段更新（数据平台凭证接入）

- 系统版本升级到 `0.10.0`。
- 后端启动时自动读取根目录 `.env`；该文件继续由 `.gitignore` 排除，不进入版本管理。
- 已识别并脱敏加载 `CDSE_S3_ACCESS_KEY`、`CDSE_S3_SECRET_KEY`、`USGS_USERNAME`、`USGS_M2M_TOKEN`；OAuth为空不影响S3和M2M流程。
- 新增 `GET /api/v1/credentials/status` 和 `POST /api/v1/credentials/validate`。接口永不返回原始密钥或USGS临时会话。
- 新增根目录 `检查数据平台密钥.bat`，可在不启动网页的情况下双击执行脱敏联网验证。
- USGS Application Token真实登录及目录查询成功，并在验证后立即注销临时会话；进一步调用`download-options`返回HTTP 403。账号仍需在ERS个人资料中申请并获批M2M API下载权限，获批前不能进入真实文件下载。
- Copernicus S3对桶列表及已知Sentinel-2对象HEAD请求均返回HTTP 403；说明当前S3凭证尚不能访问对象，需要用户在CDSE S3 Key Manager中重新生成/核对密钥或等待新密钥生效后再验证。
- GF/HJ实际产品自动检索已按用户决定退出本阶段范围；现有HJ-2B/GF-1轨道几何覆盖预测保留，并继续明确标注“不是已确认成像”。
- 后端测试更新为24项通过（凭证值不泄露、状态接口、USGS临时会话注销均有测试）。

### 当前下一步

1. 用户重新生成或核对CDSE S3密钥后再次验证。
2. 用户在USGS ERS个人资料中通过`Request Access`申请M2M API访问；获批后实现Landsat下载、真实缩略图和QA_PIXEL水库局部云量。
3. CDSE S3通过后实现Sentinel多瓦片按需下载与本地归档。
4. 最后进入生产部署与运维：API管理权限、Docker/Nginx、HTTPS、日志轮转、SQLite备份与恢复。

## 十一、2026-08-27 阶段更新（调整为只查看模式）

- 根据用户决定，取消原始影像下载、Sentinel多瓦片下载清单、USGS M2M下载和服务器自动打包功能，不再要求继续处理下载授权。
- 系统版本升级到 `0.11.0`，产品接口不再输出`download_url`或`manifest_url`，前端移除“下载”和“瓦片清单”按钮，只保留影像预览与“官方详情”。
- 删除凭证联网验证接口、检查脚本及仅为S3下载引入的`boto3`依赖；根目录`.env`仍由Git忽略，已有密钥不会显示在网页或API中。
- Sentinel-2继续使用公开官方快速预览和本地多瓦片拼接预览；水体指数与局部云量继续使用公开Earth Search COG，均不依赖用户下载凭证。
- Landsat继续展示官方目录记录；官方缩略图要求登录时，页面回退为明确标注的覆盖示意图。此限制不会被描述成真实像素预览。
- GF/HJ实际产品继续不接入，只保留轨道几何预测与官方参数说明。
- 先前“继续申请下载权限、实现原始文件归档”的待办已作废，不再属于项目未完成功能。
- 后端21项测试、前端TypeScript检查和生产构建全部通过。
- 浏览器验收丹江口19景产品：首屏8个预览全部完成加载；Sentinel拼接预览为640×480，Landsat受限项回退为240×156覆盖示意图；界面只出现“官方详情”，无下载或瓦片清单入口。

### 当前后续范围

1. 继续生产部署与运维：访问权限、Docker/Nginx、HTTPS、日志轮转、SQLite备份与恢复。

## 十二、2026-08-27 阶段更新（一键启动修复）

- 复现“点击启动没有反应”：端口5173和8000已有旧进程，其中后端仍为0.9.0；原批处理会重复启动并因端口占用退出，但没有清晰呈现错误。
- `启动网站.bat`现改由`scripts/start-app.ps1`执行幂等启动：检测已有服务、核对后端版本、仅在需要时启动，并等待前后端健康检查成功后打开网页。
- 检测到本项目旧版uvicorn占用8000时会自动切换到源码声明的当前版本；其他程序占用端口时停止并显示明确错误，不会误杀无关进程。
- PowerShell核心脚本使用ASCII文本，避免Windows PowerShell 5对无BOM中文UTF-8脚本的解析问题。
- 启动器连续执行两次验证通过：第二次正确复用服务；当前后端版本0.11.0、25座水库健康检查正常，前端HTTP 200。

## 十三、2026-08-27 阶段更新（管理接口权限）

- 系统版本升级到`0.12.0`。
- 首次启动自动生成43位随机管理员密钥到`backend/data/admin-api-key.txt`；可用环境变量`ADMIN_API_KEY`覆盖。密钥文件已加入`.gitignore`，不会进入源码或构建产物。
- 后端使用常量时间比较验证`X-Admin-Key`，无密钥或错误密钥统一返回HTTP 401；密钥不放在URL、查询参数或日志中。
- 已保护管理概览、任务记录、影像批量状态以及轨道刷新、单库/全库影像刷新、局部云量重算和水体分析重算接口。
- 地图、水库、天气、卫星窗口、影像产品和已有分析结果等只读接口继续匿名可用。
- 前端管理后台新增密码型密钥输入、错误提示、退出管理和10秒授权轮询；密钥只保存在当前浏览器会话的`sessionStorage`，不写入长期本地存储。
- 只查看页面移除会直接触发后台写入的“计算近3景”“局部云量”和首次访问自动刷新行为；后台调度仍可更新数据。
- 真实HTTP验证：公开健康接口200、未授权管理接口401、正确密钥管理接口200、未授权写接口401。
- 后端23项测试、前端TypeScript检查及生产构建通过；浏览器验证登录界面、密码遮蔽和错误密钥拒绝均正常。

### 当前剩余工作

1. Docker与Nginx/HTTPS生产部署。
2. 日志轮转。
3. SQLite自动备份与恢复。

## 十四、2026-08-27 阶段更新（部署包准备）

- 已生成后端与前端多阶段Dockerfile、基础Compose、HTTP 8080组合和HTTPS 80/443组合。
- Nginx配置包含SPA回退、`/api`反向代理、健康检查、静态缓存、gzip、安全响应头、TLS 1.2/1.3与HSTS。
- SQLite、管理员密钥和预览缓存使用命名卷`henan_reservoir_backend_data`持久化；镜像更新不会删除卷。
- 后端容器启动前自动执行`alembic upgrade head`；Compose等待后端健康后再启动Web服务。
- 部署与更新操作写入`deploy/README.md`，同时提供`.env.production.example`。
- 本机Docker Desktop因WSL版本过旧无法启动；新版WSL安装需要管理员权限。用户决定暂时跳过Docker实际构建，因此当前为“部署文件已准备、容器实测待Docker可用”。

## 十五、2026-08-27 阶段更新（日志轮转）

- 系统版本升级到`0.13.0`。
- 本地uvicorn与APScheduler写入`backend/data/logs/backend.log`，HTTP访问写入`access.log`；两者均按10 MB切分并保留10份。
- 启动器对前后端启动阶段stdout/stderr按1 MB切分并保留3份，避免启动日志长期增长。
- Docker Compose为backend与web统一设置`json-file`日志驱动：单文件10 MB、最多5份。
- 日志目录和轮转文件已加入`.gitignore`。
- 真实重启验证通过：当前0.13.0健康检查正常，APScheduler启动信息进入backend.log，HTTP请求进入access.log；23项后端测试通过。

### 当前剩余工作（更新）

1. SQLite自动备份、保留周期与恢复校验。
2. Docker可用后执行容器构建、Nginx HTTP联调；取得服务器与域名后完成公网HTTPS签发。

## 十六、2026-08-28 阶段更新（SQLite自动备份与恢复）

- 系统版本升级到`0.14.0`。
- 新增SQLite在线一致性备份：后端每天北京时间03:30自动执行，默认保留14天；可通过`DATABASE_BACKUP_ENABLED`、`DATABASE_BACKUP_HOUR`、`DATABASE_BACKUP_MINUTE`和`DATABASE_BACKUP_RETENTION_DAYS`调整。
- 备份先写入同目录临时文件，执行`PRAGMA integrity_check`并计算SHA-256后再原子改名，避免把未完成文件当成有效备份。
- 过期清理只扫描`backend/data/backups/`内直属的`.sqlite3`文件，不处理其他文件；该目录已加入`.gitignore`并位于Docker持久化数据卷范围内。
- 管理后台新增“立即备份数据库”按钮、备份份数和占用统计；`POST /api/v1/admin/backup`继续受`X-Admin-Key`保护。
- 新增根目录`备份数据库.bat`和`停止网站.bat`，以及备份列表、校验、恢复CLI。恢复主库时若后端仍运行会明确拒绝；恢复前还会自动建立`pre-restore`安全备份，并要求显式传入`--confirm-restore`。
- 恢复流程已在临时SQLite数据库上完成往返验证，包括恢复前安全备份、数据还原、损坏文件拒绝和14天过期清理；没有对当前主库执行覆盖恢复。
- 已生成第一份真实主库备份：`reservoir-observer-20260827-160052-531585.sqlite3`，大小71,790,592字节，9张表，完整性检查为`ok`，SHA-256为`847fc4c07b5f93c658964b399827c6c8f0051ffcb981c16393063a42904292ce`。
- 真实重启验证通过：当前后端为0.14.0、25座水库正常，调度器同时显示`refresh-orbits`、`refresh-all-imagery`和`backup-database`。
- 后端27项测试全部通过；前端TypeScript检查和生产构建通过。

### 当前剩余工作（更新）

1. Docker Desktop可用后执行镜像构建、Compose持久化卷和Nginx HTTP联调。
2. 获得正式服务器与域名后配置公网部署、TLS证书签发与HTTPS验收。

## 十七、2026-08-28 阶段更新（GitHub + Cloudflare无服务器改造，步骤1）

- 根据用户最终范围，部署目标调整为：保留25座水库、实时天气、未来卫星完整覆盖窗口和实验日期联合推荐；退出在线影像计算、水体面积提取、SQLite管理后台等非核心线上功能。
- 新增`backend/app/tasks/export_static_observations.py`，可复用现有CelesTrak OMM、SGP4、太阳高度角、传感器幅宽与水库多边形相交算法，将未来30天完整覆盖窗口导出为静态JSON。
- 已生成首份`public/data/orbit-passes.json`：共874条未来窗口，覆盖25/25座水库，全部来自轨道几何预测且保留`is_imaging_confirmed=false`风险说明。
- 静态数据包含北京时间日期/时刻、UTC时刻、卫星、传感器、分辨率、幅宽、覆盖率、最小轨迹距离、轨道历元及覆盖算法版本。
- 新增独立导出测试并通过；此步骤没有删除现有FastAPI后端，便于改造期间回归和结果核对。

### 本轮下一步

1. 将Vue网页切换到静态水库/轨道JSON和Cloudflare天气接口。
2. 加入GitHub Actions每日自动刷新及手动刷新入口。
3. 完成天气—过境实验推荐界面、自动测试和公网发布。

### 步骤2完成：Vue静态运行模式

- 前端已不再依赖FastAPI水库、轨道、影像、水体和管理接口；启动时只读取`/data/reservoirs.geojson`与`/data/orbit-passes.json`。
- 页面聚焦为“水库实验观测助手”：保留25座水库地图、搜索、边界和基本属性，移除影像目录、水体面积、像元分析与管理后台界面。
- 天气逻辑改为优先请求`/api/weather`（Cloudflare Worker/Pages Function），本地开发时自动回退到Open-Meteo直连；取消虚构天气数据，外部接口失败时明确显示不可用。
- 天气数据扩展为未来16天逐日预报和逐小时温度、云量、降水概率、降水量及风速，可按卫星过境时刻匹配最近小时。
- 新界面已加入可调整实验阈值（云量、降水概率、风速）、推荐窗口卡片、16天天气横向滚动和未来30天全部联合判断列表。
- 前端TypeScript检查与生产构建通过；静态构建产物不需要FastAPI或SQLite即可加载水库与轨道数据。

### 步骤3完成：GitHub Actions自动更新

- 新增`.github/workflows/refresh-orbits.yml`：每天UTC 18:15（北京时间次日02:15）及手动触发时，安装最小轨道依赖、强制获取最新CelesTrak OMM、计算未来30天窗口并自动提交`orbit-passes.json`。
- 自动更新工作流设置45分钟上限、同组任务不并发、只授予仓库内容写权限；生成后必须先通过静态观测数据校验才能提交。
- 新增`.github/workflows/validate.yml`：主分支推送和PR时使用Node 22/pnpm执行TypeScript检查与生产构建，并验证25座水库、完整覆盖率、时间格式和非确认成像语义。
- 新增`backend/requirements-orbit.txt`，轨道计算任务不安装FastAPI、Rasterio等静态站点不需要的重型依赖。
- 新增`scripts/validate_static_observations.py`与`pnpm validate:static`；本机验证结果为25座水库、874条完整覆盖窗口、覆盖25座水库，生产构建再次通过。

### 步骤4完成：Cloudflare天气函数

- 新增`functions/api/weather.ts`作为Cloudflare Pages Function：仅接受河南服务范围经纬度，自行构造固定Open-Meteo参数，不允许调用者把它当成任意URL代理。
- 天气函数返回未来16天逐日和384小时逐小时数据，采用北京时间、m/s风速，边缘缓存2小时并允许上游短时故障期间使用24小时陈旧缓存策略。
- 新增`functions/api/health.ts`，用于部署后确认Cloudflare函数、版本和UTC时间正常。
- 新增`public/_routes.json`，仅`/api/*`进入函数；静态资源与数据不消耗函数请求。新增`public/_headers`，设置CSP、安全响应头以及静态资源/轨道JSON缓存策略。
- 新增独立Cloudflare函数TypeScript配置与类型检查；本机Wrangler兼容运行验证通过：健康接口200，彰武水库天气返回16天/384小时，越界坐标返回400。
- 本地Wrangler只读取`.env`用于开发进程且所有值保持隐藏；`.env`继续被Git忽略，Cloudflare生产部署不会上传这些历史影像密钥，天气和轨道功能本身也不需要任何数据平台密钥。

### 步骤5完成：天气—卫星过境联合推荐

- 新增独立推荐模块`src/services/recommendation.ts`，将每次完整覆盖过境与时间最接近的逐小时天气自动匹配，并综合云量、降水概率、降水量和风速计算0—100分。
- 推荐状态分为“推荐”“备选”“不推荐”和“待预报”：只有同时满足用户设置的云量、降水概率、风速阈值且综合评分不低于75分，才标记为推荐实验窗口。
- 超出Open-Meteo未来16天预报范围的轨道窗口保留显示，但明确标记“待预报”，不使用虚构天气，也不提前作出推荐。
- 页面中的推荐卡片和全部过境列表共用同一推荐模块，修改阈值后即时重新计算，避免两处判断规则不一致。
- 新增4项Vitest单元测试，覆盖最近小时匹配、适宜天气推荐、超出预报范围和高风险天气拒绝；测试全部通过。
- GitHub验证工作流已加入单元测试，并将pnpm版本与当前锁文件生成环境统一为11.19.0。
- 本机完整验证通过：4项推荐测试、Cloudflare函数TypeScript检查、25座水库/874条完整覆盖窗口数据校验以及Vue生产构建均成功。

### 本轮下一步

1. 使用真实浏览器对地图、25座水库、16天天气、横向滚动、卫星筛选、推荐结果及移动端布局进行端到端验收。
2. 整理静态部署说明与代码仓库，确认没有密钥或本地数据库被纳入版本控制。
3. 创建/连接GitHub仓库和Cloudflare Pages项目，完成公网部署并验证线上天气接口。

### 步骤6完成：自动测试与真实浏览器端到端验收

- 自动验证全部通过：推荐算法4项单元测试、轨道静态导出1项回归测试、Cloudflare函数TypeScript检查、25座水库/874条过境窗口数据校验及Vue生产构建均成功。
- 使用本地Wrangler按Cloudflare Pages实际运行方式进行浏览器验收，健康接口、实时天气接口和静态数据均正常，浏览器控制台无错误或警告。
- 浏览器检查确认页面加载25座水库、16张带“月/日+周几”的天气卡片；天气容器`overflow-x:auto`且实际内容宽度大于可视宽度，左右按钮可将滚动位置从0推进到约333px。
- 发现并修复地图初始化生命周期缺陷：原先地图容器在加载态结束前尚未进入DOM，Leaflet初始化会提前返回；现已先结束加载态、等待Vue渲染，再创建地图。
- 修复后实测OpenStreetMap底图加载12张可见瓦片，25个水库边界全部绘制，地图缩放控件正常；切换丹江口水库后标题、选中状态、地图范围、天气更新时间与推荐卡片同步刷新。
- 卫星筛选实测通过：选择`Landsat 8`后联合判断列表只显示Landsat 8；云量阈值改为0后推荐卡片即时重新计算为0个，证明阈值联动有效。
- 390×844移动视口实测通过：手机菜单显示、侧栏抽屉开关正常、地图高度321px、详情栏适配可视宽度且不存在页面横向溢出。

### 当前剩余工作（更新）

1. 重写部署说明并检查待提交文件，确保`.env`、SQLite数据库、备份和构建缓存不会进入GitHub仓库。
2. 创建或连接GitHub仓库并推送代码。
3. 在Cloudflare Pages连接该仓库，执行首次构建发布，最后验证公网首页、`/api/health`和实时天气。

### 步骤7A完成：公开仓库整理与账号连接检查

- README已重写为GitHub + Cloudflare静态公开版说明，包含系统架构、本地运行、自动测试、GitHub Actions、Cloudflare Pages构建参数、上线验收地址和数据风险说明。
- `package.json`已固定`pnpm@11.19.0`并要求Node.js 22，降低Cloudflare和GitHub构建环境差异。
- Git忽略规则已补齐：`.env`、全部`backend/data/`、SQLite、备份、日志、影像缓存、`.wrangler`、虚拟环境、Node依赖、构建产物、测试缓存、TypeScript缓存和原始Word方案均不会提交。
- 已初始化本地`main`分支仓库并暂存待发布内容，共93个文件、约1.66 MB；最大文件为水库GeoJSON和轨道JSON，没有超大二进制缓存。
- 对暂存内容执行敏感文件与典型凭据模式扫描，未发现真实API密钥、Token或私钥；`.env.production.example`只有空的示例变量。
- Cloudflare Chrome会话已确认登录到现有账号，可继续创建Pages项目；GitHub插件已连接但授权仓库列表为空，内置浏览器和Chrome中的GitHub网页均尚未登录。
- GitHub登录页与Cloudflare控制台已在Chrome中保留。下一步需要用户亲自完成GitHub登录（账号、密码、验证码不会由系统读取或代填），然后才能确定Git提交身份、创建仓库、推送并让Cloudflare连接该仓库。

### 步骤7B完成：GitHub仓库与Cloudflare Pages公网发布

- 已在GitHub账号`Sss-gc`创建公开仓库`henan-reservoir-observation-assistant`，本地`main`已推送并跟踪`origin/main`。
- Git提交身份使用`Sss-gc@users.noreply.github.com`，避免在公开提交中暴露个人邮箱。
- Cloudflare Workers and Pages GitHub App仅授权访问本项目仓库，没有授予当前或未来全部仓库权限。
- 已创建同名Cloudflare Pages项目，生产分支为`main`，构建命令为`pnpm build`，输出目录为`dist`。
- 首次部署成功，公网地址为`https://henan-reservoir-observation-assistant.pages.dev/`。
- 线上浏览器验收通过：页面标题、25座水库、Leaflet地图、16天天气、推荐窗口和全部联合判断正常，控制台无错误或警告。
- `GET /api/health`返回`status=ok`、版本`1.0.0`；天气接口返回Open-Meteo来源、16天逐日和384小时逐小时数据，时区为`Asia/Shanghai`。
- 验收时发现README天气示例误写为`lat/lon`，已修正为接口实际使用的`latitude/longitude`。
