# Public host name used by Nginx. Use _ for an IP-only HTTP deployment.
DOMAIN_NAME=_

# Optional. If empty, the backend creates a persistent random key in its data volume.
ADMIN_API_KEY=

SCHEDULER_ENABLED=true
SCHEDULER_IMAGERY_DAYS=90
SCHEDULER_IMAGERY_LIMIT=500
